{
  "name": "level-one-pdf-converter",
  "version": "0.1.0",
  "description": "Offline, air-gapped PDF to Markdown converter — single HTML file",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "pdfjs-dist": "^3.11.174",
    "tesseract.js": "^5.1.1",
    "marked": "^12.0.0"
  }
}
